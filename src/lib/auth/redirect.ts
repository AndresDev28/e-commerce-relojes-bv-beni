/**
 * Pure helper that validates and sanitizes a redirect target.
 * Returns a safe same-origin path or the default fallback `/mi-cuenta`.
 */
export function sanitizeRedirect(value: string | null | undefined): string {
  // Stub: TDD RED phase — production code not yet written.
  // All test assertions below must fail.
  return ''
}
