/**
 * Image URL normalization constants.
 *
 * Keep this module dependency-free so it can be imported from
 * both the helper and any UI surface that needs the placeholder
 * without pulling a transitive tree.
 */

export const PLACEHOLDER_SRC = '/images/no-image.svg' as const
