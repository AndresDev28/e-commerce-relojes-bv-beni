/**
 * Barrel export para utilities de emails
 * Permite importar todo desde un solo lugar
 */

export { renderEmailToHtml } from './renderEmail';
export { colors, spacing, typography, container, button } from './styles';
