/**
 * Barrel export para componentes de emails
 * Permite importar todos los componentes desde un solo lugar
 * 
 * @example
 * import { EmailHeader, EmailFooter, StatusBadge } from '@/emails/components'
 */

export { EmailHeader } from './EmailHeader';
export { EmailFooter } from './EmailFooter';
export { StatusBadge } from './StatusBadge';
export { OrderItems } from './OrderItems';
export { OrderSummary } from './OrderSummary';
